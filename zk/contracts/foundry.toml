[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200
# The test suite reads the committed proof fixture.
fs_permissions = [{ access = "read", path = "./test/fixtures" }]
